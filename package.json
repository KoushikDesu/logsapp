{
  "name": "logsapp",
  "version": "1.0.0",
  "description": "LogsApp / RoyalChat - Real-Time Messenger with 1GB File Bridge, Supabase PostgreSQL, Auto-Purge Storage, and Linux CLI Companion",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "cd client && npm install && npm run build",
    "build:client": "cd client && npm install && npm run build",
    "build:server": "cd server && npm install && npm run build",
    "build:all": "cd client && npm install && npm run build && cd ../server && npm install && npm run build",
    "vercel-build": "cd client && npm install && npm run build",
    "start": "node server/dist/index.js",
    "migrate": "python server/src/db/migrate.py"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^6.0.1"
  },
  "keywords": [
    "chat",
    "royalchat",
    "logsapp",
    "supabase",
    "postgresql",
    "cli",
    "linux",
    "real-time",
    "file-sharing",
    "websockets"
  ],
  "author": "Koushik Desu",
  "license": "MIT"
}
